when:
  - event: push
  - event: pull_request
  - event: manual

labels:
  host: nas
  tethux-canary: "true"
  privileged: "true"

steps:
  full-ci:
    image: nixos/nix
    commands:
      - nix develop .#ci --extra-experimental-features "nix-command flakes" -c ./nix/scripts/ci.sh
